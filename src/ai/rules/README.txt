Add new expert heuristics here. Each rule is a lowercase trigger phrase mapped to a concise recommendation. The current engine loads its rules statically; future versions can parse this file to refresh at runtime.

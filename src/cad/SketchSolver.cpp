#include "SketchSolver.h"

bool SketchSolver::solve(SketchDocument& doc, int iterations, double alpha)
{
    (void)doc;
    (void)iterations;
    (void)alpha;
    // TODO: implement actual constraint solving algorithm
    return true;
}

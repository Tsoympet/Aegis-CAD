"""Reverse engineering helper script (stub).

In a real setup this would:
- Accept images + text specs
- Run a local multimodal model
- Emit a neutral JSON recipe for AegisReverseEngine
"""

def run_reverse(spec_path: str) -> None:
    print(f"[reverse_agent] called with {spec_path}")
